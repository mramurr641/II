/* Con este programa se aprende a:
*  - Estructura basica de un programa en C.
*  - Uso de librerias.
*  - Usar comentarios de linea y comentarios de parrafo (Esto es un comentario de parrafo).
*  - Mostrar mensajes por la consola. 
*/

#include <stdio.h> // libreria: permite mostrar mensajes por pantalla y solicitar valores al usuario 

int main() { // INICIO (Esto es un comentario de linea)
    printf("Hello World"); // Muestra Hello World en la consola al dar a Run Code (Icono de Play)
    return 0;              // Buena practica de programacion en C: esta linea no es obligatoria,
                           // pero permite hacer saber a la funcion main() que ha concluido
                           // correctamente
} // FIN