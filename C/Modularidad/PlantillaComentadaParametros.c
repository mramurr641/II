#include <stdio.h>
int main() { // La funcion no tiene parametros de entrada. No pone
             // el tipo de dato y nombre de ninguna variable dentro
             // de los parentesis que acompanan al nombre de la
             // funcion main.
    
    return 0;
}