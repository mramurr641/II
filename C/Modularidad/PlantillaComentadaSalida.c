#include <stdio.h>
int main() { // La funcion es de tipo int pues devuelve un entero
    
    return 0; // La funcion devuelve el valor 0 de tipo entero.
              // Esto se hace para indicarle al IDE que todo ha
              // salido bien en la funcion main.
}